const SearchButton = () => {
    const style = {
        background: "blue",
        color: "white"
      }

    return (
        <button style={style}>Search</button>
    )
}

export default SearchButton